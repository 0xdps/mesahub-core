{
  "name": "sqlite-hub-client",
  "version": "0.1.0",
  "description": "Lightweight client SDK for sqlite-hub — SQLite over HTTP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc"
  },
  "keywords": ["sqlite", "sqlite-hub", "railway"],
  "license": "MIT",
  "dependencies": {
    "@pingpong-js/fetch": "^1.0.2"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
